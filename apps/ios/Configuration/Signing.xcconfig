// Shared signing boundary. This file affects the build and is pinned.
// Personal Team IDs belong only in the optional ignored include.
#include "ReleaseIdentity.generated.xcconfig"
DEVELOPMENT_TEAM =
POWER_SOURCE_REVISION =
POWER_RELEASE_REHEARSAL = NO
#include? "LocalSigning.xcconfig"
